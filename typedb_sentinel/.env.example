# .env.example

# --- API do Google Gemini ---
# Obtenha sua chave em https://ai.google.dev/
GEMINI_API_KEY="sua-chave-api-gemini-aqui"

# --- Configuração do TypeDB MCP Server ---
# URL do seu servidor. Use wss:// para produção com TLS.
TYPEDB_SERVER_URL="ws://localhost:8787/mcp/ws"

# Token JWT com permissões de administrador para a maioria dos testes.
TYPEDB_ADMIN_TOKEN="seu-token-de-admin-jwt-aqui"

# --- Configurações Opcionais ---

# Token JWT com permissões restritas (ex: apenas leitura) para os testes de segurança.
# Deixe em branco se não for realizar testes de segurança.
TYPEDB_SECURITY_TOKEN="seu-token-de-seguranca-jwt-aqui"

# Caminho para o certificado da CA se você estiver usando TLS com um certificado autoassinado.
# Deixe em branco para conexões 'ws://' ou se estiver usando uma CA pública.
# Exemplo: TYPEDB_TLS_CA_PATH="./certs/my_dev_ca.pem"
TYPEDB_TLS_CA_PATH=