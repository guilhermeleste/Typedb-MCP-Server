# =============================================================================
# Typedb-MCP-Server .gitignore - Comprehensive Security Configuration
# =============================================================================
# This file ensures NO sensitive data (credentials, keys, certificates, 
# secrets, tokens, passwords) are committed to git. For onboarding new 
# developers, corresponding .example files are provided.
# =============================================================================

# -----------------------------------------------------------------------------
# Build Artifacts and Binaries
# -----------------------------------------------------------------------------
/target/
**/*.rlib
**/*.d
**/*.o
**/*.so
**/*.dll
**/*.dylib
**/*.exe
Cargo.lock

# -----------------------------------------------------------------------------
# Temporary Files and Editor Artifacts
# -----------------------------------------------------------------------------
*.log
*.tmp
*.swp
*.swo
*.bak
*.old
*.orig
*.rej
*.DS_Store
*~

# -----------------------------------------------------------------------------
# SECURITY: Environment Variables and Sensitive Configuration
# -----------------------------------------------------------------------------
# Ignora todos os arquivos .env exceto templates/examples
.env
.env.*
!.env.example
!.env.template

# Configuração local e arquivos sensíveis
config.toml
config.local.toml
*.local.toml
local-config.toml
production.toml
staging.toml
*config*.local.toml

# CRITICAL: Qualquer arquivo com 'secret', 'password', ou 'credential' no nome
*secret*
*password*
*credential*
*token*
*auth-config*
!*.example.*
!*.template.*

# -----------------------------------------------------------------------------
# SECURITY: Vault and Secret Management Files
# -----------------------------------------------------------------------------
# Vault AppRole credentials (NEVER commit these)
role_id
role_id.txt
secret_id
secret_id.txt

# Vault token files
.vault-token
vault-token*
vault_token*

# Secret directories (entire directories)
/test-secrets/
/dev-secrets/
/local-secrets/
/vault-secrets/
/local-dev-secrets/
/secrets/

# Vault configuration with real credentials
vault-config.hcl
vault-agent.hcl
!vault-agent-config.hcl
!*example*
!*template*

# -----------------------------------------------------------------------------
# SECURITY: Certificates, Keys and Cryptographic Materials
# -----------------------------------------------------------------------------
# Private keys (ALL patterns)
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
*.truststore
*.pkcs12

# Certificates (generated/dev/test)
*.crt
*.cer
*.der
*.csr

# Certificate directories (protect generated certs)
/certs/generated/
/certs/generated-dev/
/certs/local/
/certs/dev/
/certs/test/

# ALLOW: Example certificates (for reference only)
!/certs/example-certs/
!/certs/example-certs/**

# Test certificates and keys (dev/test environments)
/tests/test_certs/
/tests/common/test_keys/
/tests/**/certs/
/tests/**/keys/

# ALLOW: Keep example/template files for onboarding
!*.example.key
!*.example.pem
!*.example.crt
!**/example-*
!**/template-*

# -----------------------------------------------------------------------------
# IDE and Editor Configurations
# -----------------------------------------------------------------------------
/.vscode/
/.idea/
/.github/
/.repomix/
*.code-workspace
/.docker-cache/
*.sublime-workspace
*.sublime-project

# -----------------------------------------------------------------------------
# Test Artifacts and Development
# -----------------------------------------------------------------------------
# Test output and temporary files
/tests/**/output/
/tests/**/tmp/
/tests/**/mock/

# Test configuration with sensitive data
/tests/**/test-config.toml
tests/test_configs/*.local.toml

# Coverage and benchmark results
/coverage/
*.lcov
/bench/

# -----------------------------------------------------------------------------
# Documentation and Generated Content
# -----------------------------------------------------------------------------
/target/doc/
/project_info/
/thinking/

# -----------------------------------------------------------------------------
# Development and Deployment Artifacts
# -----------------------------------------------------------------------------
# Lock files and backups
Cargo.lock.bak
*.lock

# Build artifacts (C/C++)
*.obj
*.a
*.lib
*.exp
*.pdb
*.idb
*.ilk
*.aps
*.ncb
*.sdf
*.opensdf
*.tlog
*.vspscc
*.vssscc
*.scc

# Cache directories
/.cargo/
/.rustup/
__pycache__/
*.pyc
*.pyo
*.pyd

# CI/CD and deployment
/ci-artifacts/
/scripts/output/
/deploy/
/production/
/staging/
/development/
/integration/

# -----------------------------------------------------------------------------
# Debug and Core Dumps
# -----------------------------------------------------------------------------
*.core
*.dump
*.stackdump

# -----------------------------------------------------------------------------
# Database Files (may contain sensitive data)
# -----------------------------------------------------------------------------
*.sqlite
*.db
*.db-journal

# -----------------------------------------------------------------------------
# Vendor and Dependencies
# -----------------------------------------------------------------------------
/vendor/

# -----------------------------------------------------------------------------
# SECURITY: Additional Protection Patterns
# -----------------------------------------------------------------------------
# JWT files and tokens
*.jwt
*.token
*jwt*
*oauth*secret*

# API keys and tokens in any format
*api*key*
*access*token*
*refresh*token*

# Docker secrets (if accidentally copied locally)
*docker*secret*
*compose*secret*

# Backup files that might contain sensitive data
*.backup
*.bkp
*backup*

# Any file with 'private' in the name (except examples)
*private*
!*.example.*
!**/example-*

# SSH and other authentication files
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
known_hosts
authorized_keys
.ssh/

# -----------------------------------------------------------------------------
# EXCEPTIONS: Files That Should Be Tracked
# -----------------------------------------------------------------------------
# Always track example/template files for onboarding
!*.example
!*.template
!example-*
!template-*
!**/examples/
!**/templates/

# Track specific config files that are templates
!configs/config.example.toml
!configs/config.dev.toml
!configs/config.test.toml
!configs/.env.example

# Track test configurations that don't contain secrets
!tests/test_configs/default.test.toml
!tests/test_configs/oauth_enabled.test.toml
!tests/test_configs/server_tls.test.toml
!tests/test_configs/typedb_*.test.toml

# Track mock/example data files
!mock_*.json
!**/mock/**
!**/mocks/**

# Track documentation and reference materials
!docs/
!README*
!CONTRIBUTING*
!LICENSE*
!CODE_OF_CONDUCT*

# Allow specific tool configurations that are not sensitive
!clippy.toml
!rustfmt.toml  
!rust-toolchain.toml
memory.md
