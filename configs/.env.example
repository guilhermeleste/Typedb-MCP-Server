# =============================================================================
# Typedb-MCP-Server Environment Variables (Post-Vault Architecture)
# =============================================================================
# Este arquivo define variáveis de ambiente para CONFIGURAÇÕES NÃO-SENSÍVEIS.
# Com a integração do Vault, os SECRETS são gerenciados pelo Vault Agent.
#
# IMPORTANTE: Este arquivo NÃO deve conter secrets, senhas, tokens ou chaves!
# Copie para .env e ajuste conforme necessário para desenvolvimento local.
# =============================================================================

# -----------------------------------------------------------------------------
# 🔐 ARQUITETURA DE SECRETS (VAULT-FIRST)
# -----------------------------------------------------------------------------
# SECRETS SÃO GERENCIADOS PELO VAULT:
# - Senhas do TypeDB: Renderizadas em /vault/secrets/db_password.txt
# - Tokens de autenticação: Gerenciados via AppRole
# - Chaves criptográficas: Armazenadas no Vault KV store
#
# OS ARQUIVOS .ENV AGORA CONTÊM APENAS:
# - Configurações de desenvolvimento não-sensíveis
# - Sobrescritas de configuração TOML
# - Ajustes de logging e debugging
# - Caminhos de arquivos de configuração

# -----------------------------------------------------------------------------
# 📁 CONFIGURAÇÃO DE ARQUIVOS
# -----------------------------------------------------------------------------
# Caminho para arquivo de configuração TOML alternativo
# Útil para diferentes ambientes de desenvolvimento
# Default: "typedb_mcp_server_config.toml"
MCP_CONFIG_PATH=

# Exemplo para ambiente de desenvolvimento específico:
# MCP_CONFIG_PATH="config/dev-local.toml"

# -----------------------------------------------------------------------------
# 🔧 LOGGING E DEBUGGING  
# -----------------------------------------------------------------------------
# Nível de log para aplicação e dependências
# Formato: <nível_padrão>[,<crate>=<nível>...]
# Níveis: error, warn, info, debug, trace
RUST_LOG=info

# Exemplos de configuração de log:
# RUST_LOG="debug"                           # Debug geral
# RUST_LOG="info,typedb_mcp_server=debug"    # Debug apenas do nosso crate
# RUST_LOG="warn,typedb_driver=trace"        # Trace do driver TypeDB

# -----------------------------------------------------------------------------
# ⚙️ SOBRESCRITAS DE CONFIGURAÇÃO (MCP_ prefix)
# -----------------------------------------------------------------------------
# Sobrescrever configurações do arquivo TOML usando variáveis de ambiente
# Formato: MCP_<SEÇÃO>__<CAMPO>=<valor>
# Útil para ajustes pontuais sem modificar arquivos .toml

# --- Configurações do Servidor ---
# MCP_SERVER__BIND_ADDRESS="127.0.0.1:8080"     # Endereço de bind alternativo
# MCP_SERVER__TLS_ENABLED=false                 # Desabilitar TLS para dev
# MCP_SERVER__TLS_CERT_PATH="/custom/cert.pem"  # Certificado customizado

# --- Configurações do TypeDB ---
# MCP_TYPEDB__ADDRESS="localhost:1729"          # Servidor TypeDB local
# MCP_TYPEDB__USERNAME="admin"                  # Usuário alternativo
# MCP_TYPEDB__TLS_ENABLED=false                 # TLS do TypeDB para dev

# --- Configurações de Autenticação ---
# MCP_AUTH__OAUTH_ENABLED=false                 # Desabilitar OAuth para dev local
# MCP_AUTH__OAUTH_JWKS_URI="http://localhost:8080/.well-known/jwks.json"

# --- Configurações de Métricas ---
# MCP_METRICS__ENABLED=true                     # Habilitar métricas Prometheus
# MCP_METRICS__BIND_ADDRESS="127.0.0.1:9090"   # Endpoint de métricas

# -----------------------------------------------------------------------------
# 🛠️ DESENVOLVIMENTO LOCAL ESPECÍFICO
# -----------------------------------------------------------------------------
# Configurações úteis apenas para desenvolvimento local

# Desabilitar verificação de certificados TLS (APENAS PARA DEV!)
# MCP_TYPEDB__TLS_ENABLED=false
# MCP_SERVER__TLS_ENABLED=false

# Usar configuração de teste para desenvolvimento rápido
# MCP_CONFIG_PATH="config.test.toml"

# Logging verboso para debugging
# RUST_LOG="debug,typedb_mcp_server=trace"

# -----------------------------------------------------------------------------
# 🚫 O QUE NÃO COLOCAR NO .ENV (AGORA GERENCIADO PELO VAULT)
# -----------------------------------------------------------------------------
# ❌ TYPEDB_PASSWORD - Agora renderizado pelo Vault em /vault/secrets/
# ❌ JWT secrets/keys - Armazenados no Vault KV store  
# ❌ TLS private keys - Gerenciados por certificados externos
# ❌ OAuth client secrets - Configurados via Vault
# ❌ AppRole credentials - Montados via Docker secrets
# ❌ Qualquer token ou chave criptográfica

# -----------------------------------------------------------------------------
# 📚 MIGRAÇÃO DE CONFIGURAÇÕES LEGADAS
# -----------------------------------------------------------------------------
# Se você tinha configurações sensíveis em .env antes do Vault:
# 1. Remova TODAS as senhas, tokens e chaves deste arquivo
# 2. Configure secrets no Vault conforme documentação
# 3. Use apenas as variáveis não-sensíveis documentadas acima
# 4. Consulte test-secrets/README.md para setup de desenvolvimento
